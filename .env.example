GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=
GITHUB_REPO=owner/my-blog
ALLOWED_GITHUB_USER=jcjang90
SESSION_SECRET=change-this-to-random-32-char-string

# Public site URL (used by SEO, RSS, sitemap, JSON-LD, OG tags).
# Override per-environment to point canonical/social tags at the right origin.
# - Cloudflare Pages: set in Settings -> Environment variables (build).
# - Local dev: copy this file to .env and adjust if needed.
# Trailing slashes are stripped automatically.
VITE_SITE_URL=https://jcjang-blog.pages.dev
